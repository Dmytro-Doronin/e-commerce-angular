import { FiltersForSliderPipe } from './filters-for-slider.pipe'

describe('ImagesForSliderPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltersForSliderPipe()
    expect(pipe).toBeTruthy()
  })
})
