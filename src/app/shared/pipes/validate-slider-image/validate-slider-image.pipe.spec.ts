import { ValidateSliderImagePipe } from './validate-slider-image.pipe'

describe('ValidateSliderImagePipe', () => {
  it('create an instance', () => {
    const pipe = new ValidateSliderImagePipe()
    expect(pipe).toBeTruthy()
  })
})
