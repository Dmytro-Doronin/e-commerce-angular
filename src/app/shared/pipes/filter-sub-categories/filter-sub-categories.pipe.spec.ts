import { FilterSubCategoriesPipe } from './filter-sub-categories.pipe'

describe('FilterSubCategoriesPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterSubCategoriesPipe()
    expect(pipe).toBeTruthy()
  })
})
